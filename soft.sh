#! /usr/bin/env bash

# DEPRECATED: All packages have been migrated to config.toml
# Use the settle tool to install packages from config.toml instead
#
# See: ~/dotfiles/config.toml

echo "This script is deprecated. All packages are now managed in config.toml"
echo "Please use the settle tool to install packages."
exit 1
