# with option enabled keys used by ssh will be automatically added to ssh-agent. 
# No need to call ssh-add
AddKeysToAgent yes

# Exit automatically on network interuptions:
# poke server every 5 seconds
ServerAliveInterval 5
# allow to alive-check to fail just once, on second attempt it will time out
ServerAliveCountMax 1

